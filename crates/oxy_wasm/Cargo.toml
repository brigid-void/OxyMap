[package]
name = "oxy_wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"
flatgeobuf = "0.4"
serde = { version = "1.0", features = ["derive"] }
